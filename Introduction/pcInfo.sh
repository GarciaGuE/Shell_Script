#!/bin/bash

# Display a welcome message with the username and user identifier
echo "Welcome, $USER! Your user identifier is $UID"

# Display information about the shell, its level, and the time it has been running
echo "This is shell number $SHLVL, which has been running for $SECONDS seconds"

# Display information about the system architecture and the terminal client
echo "The architecture of this machine is $MACHTYPE and the terminal client is xterm"
